# 项目规则（Hotspot System）

## UI 语言规则
- Web UI 的用户可见文案默认使用中文（简体）。
- 以下专有名词可保留英文：`AppsFlyer`、`SKAN`、`API`、`Webhook`、`Feishu`、`app_key`、`pull_app_id`、`dataset`、`metric`、`event_name`、`timezone`、`JSON`。
- 新增或修改 UI 页面时，必须保持该规则一致。

## 文档语言建议
- 面向内部运营与研发的操作文档优先中文。
- 涉及接口字段名时可中英混排，字段名保持原样。
